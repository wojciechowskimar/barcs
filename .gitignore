# Lokalna baza danych (cache) - nie commitujemy danych rynkowych
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Logi
logs/*.log

# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Streamlit
.streamlit/secrets.toml

uruchom_barcs.bat
CLAUDE.md

# Paczka handoffu designu (jednorazowy input - README/CSS/PY stąd już
# wdrożone w assets/ i src/ui/; reference/ to prototypy React "tylko do
# oglądania" i duplikat logo.png, nie część aplikacji).
design_handoff_streamlit_theme/

# Paczka handoffu migracji Dash (jednorazowy input - scaffold/ stąd już
# wdrożony w dash_app.py, src/ui/, src/views/, src/data/, assets/barcs.css;
# reference/ to prototypy/UI kit "tylko do oglądania", nie część aplikacji).
dash_migration_handoff/

