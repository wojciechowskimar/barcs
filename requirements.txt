# Streamlit: PRZYPIĘTE dokładnie (nie >=) - src/ui/barcs_theme.css celuje w
# konkretne wewnętrzne data-testid tej wersji (np. stSelectboxVirtualDropdown,
# stMultiSelectDropdown, stNumberInputContainer, stDateInputField), część z
# nich zmieniona przez Streamlit z BaseWeb na React Aria Components dopiero
# w tej generacji wydań. Aktualizacja Streamlita może po cichu zepsuć motyw
# (patrz src/ui/barcs_theme.py) - po każdej podbitce wersji przejrzyj wzrokowo
# wszystkie 6 zakładek w OBU motywach (dark/light, theme_toggle()).
yahooquery>=2.3.7
yfinance>=0.2.40
pandas>=2.2.0
numpy>=1.26.0
streamlit==1.62.0
plotly>=5.22.0
tqdm>=4.66.0
requests>=2.31.0
lxml>=5.0.0

# Dash (migracja z Streamlita - patrz dash_migration_handoff/). Obie wersje
# stoją obok siebie na razie: `streamlit run app.py` i `python dash_app.py`.
# Streamlit i .streamlit/ znikną z tego pliku dopiero w kroku 7 migracji,
# gdy wszystkie sześć widoków będzie działać w Dash.
dash==2.18.2
dash-ag-grid==31.3.0
